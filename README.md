# 奶龙联络处

一个无需构建步骤的单页随机文案生成器，献给珍视奶龙的人。

## 功能

- 随机抽取奶龙主题文案
- 显示本次页面访问中的抽取次数
- 一键复制当前文案
- 适配桌面和移动端浏览

## 使用方法

直接用浏览器打开 `index.html` 即可使用；也可以在项目目录启动任意静态文件服务器后访问它。

例如：

```bash
python3 -m http.server 8000
```

然后打开 <http://localhost:8000>。

## 技术

纯 HTML、CSS 和 JavaScript，无第三方依赖。

